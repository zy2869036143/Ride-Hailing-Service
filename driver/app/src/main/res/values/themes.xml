<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Driver" parent="Theme.MaterialComponents.DayNight.DarkActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Driver.Fullscreen" parent="Theme.Driver">
        <item name="android:actionBarStyle">@style/Widget.Theme.Driver.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.Driver.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
    <declare-styleable name="ButtonSliding">
        <!--        弧的颜色-->
        <attr name="boundColor" format="color"></attr>
        <!--        文本的颜色-->
        <attr name="mtextColor" format="color"></attr>
        <!--        按钮是选择图片还是默认的颜色按钮形式，默认是颜色，设置true之后需设置btnSrc-->
        <attr name="typeColor" format="boolean"/>
        <!--        按钮的图片或者颜色，只有在typeColor设置为true时有效-->
        <attr name="btnSrc" format="reference|color"></attr>
    </declare-styleable>

    <style name="Theme.Driver.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Driver.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Driver.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>